class Main 
  def main() : Fut void
    let other = new Other in
      {other.init(10);
       other.work()}

class Other
  count : int
  def init(va : int) : void
    this.count = va
  def work() : void
    {this.count = this.count - 1;
     this.count = this.count - 1;
     this.count = this.count - 1;
     print this.count;
     print "Hello Ponyworld!"}
