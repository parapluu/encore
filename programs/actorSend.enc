class Main 
  def main() : void
    let other = new Other in
      let another = new Other in
        {other.init(another);
         other.work()}

class Other 
  other : Other
  def init(va : Other) : void 
    this.other = va
  def work() : void
    {let othertmp = this.other in
       {othertmp.printMsg();
        print "sent"}}
  def printMsg() : void
    print "Hello Actorworld!"
