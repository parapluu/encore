class Main {
  def main() : Object
    let other : Other = new Other in
       let another : Other = new Other in
         {other.init(another);
          other.work()}
}
class Other {
  other : Other
  def init(va : Other) : void 
    this.other = va
  def work() : void
    {print Other this.other;
     let othertmp : Other = this.other in
       {othertmp.printMsg();
        print string "sent"}}
  def printMsg() : void
    print string "Hello Actorworld!"
}