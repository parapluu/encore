-- A boxed Real number.
passive class Real {
  value : real
  def init(x: real) : void {
    this.value = x;
  }

  def show() : String {
    string_from_real(this.value)
  }

  def value() : real {
    this.value
  }
}
