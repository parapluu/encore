import Ped_util.Global_funs
import Ped_util.Regions
import Ped_util.Quad_tree


active class Main
  def main(args : [String]) : unit
    let
      agents = parse_file("16000_scenario.xml")
      boxes = 1
      steps = 10000
    in
     regions(agents,boxes,steps)
    end
  end
end
