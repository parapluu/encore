def id(x:int) : int
  x

class Test
  def f() : int
    1

  def g(f: int -> int) : int
    f(this.f())

class Main
  def main(): void{
    var v = 0;
    repeat i <- 1000 {
      val t = new Test;
      get t.f();
      val f = \(x:int) -> id(x);
      v = v + get t.g(f);
    };
    print v;
  }
