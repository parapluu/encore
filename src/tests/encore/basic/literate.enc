#+literate
* The test
#+begin_src encore 
bundle literate where
#+END_SRC

** This block tests whether non-literate imports work
#+begin_src encore
import String -- note: this is a non-literal import
import Random -- note: this imports a literate bundle
#+end_src

#+BEGIN_SRC ruby
class Main
  def main
    puts "This Ruby program will be ignored"
  end
end
#+END_SRC

get is a reserved word in Encore. Luckily, this text will not be compiled.

** This block does not use the encore mode
#+begin_src 
class Printer
  def print(): void
    print("Hello, literally!")
#+end_src

** This block uses the encore mode
#+begin_src encore
class Main
  def main(): void
    let p = new Printer() in p ! print()
#+end_src


# Local Variables:
# mode: org
# End:
