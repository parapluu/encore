def foo() : int
  ping(42)
where
  def ping(x : int) : int
    if x == 0
    then 0
    else 1 + pong(x - 1)
  def pong(x : int) : int
    if x == 0
    then 0
    else 1 + ping(x - 1)
end

class Main
  def main() : void
    print foo()