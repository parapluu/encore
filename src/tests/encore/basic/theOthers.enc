-- This file was automatically converted by encorec

class Main
  def main() : unit
    let
      other = new Other(10)
    in
      other!work()
    end
  end
end
class Other
  var count : int
  def init(va : int) : unit
    this.count = va
  end
  def work() : unit
    this.count = this.count - 1
    this.count = this.count - 1
    this.count = this.count - 1
    println(this.count)
    println("Hello Ponyworld!")
  end
end
