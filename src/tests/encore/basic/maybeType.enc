def fun(x: Maybe t): Maybe t {
  Nothing
}

def nothing(x: Maybe int) : Maybe int {
  let y = Nothing : Maybe int
      z = Just 32
  in {
    x = y;
    y = Just 23;
    y = x;
    y = z;
    x = Nothing;
    y;
    match x with {
      z => 42;
    };
  };
  x
}

def other(x: Maybe int): Maybe int
  x

class Test
  z: Maybe int

  def init(x: Maybe int): void
    this.z = x

  def other(x: Maybe int) : Maybe int
    x

class Main
  def main(): void {
    let x = Nothing
        y = Just 24
        z = new Test(y)
    in {
      new Test(x);
      nothing(y);
      nothing(Nothing);
      nothing(Just 32);
      };
    fun(Nothing);
    match x with {
	 Just 32 => 23;
         Nothing => 22;
    };
    Nothing
  }
