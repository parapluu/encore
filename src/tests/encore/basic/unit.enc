class Foo
  f : void
  def foo(unit : void) : void
    print "Got unit"

class Main
  def main() : void
    let x = new Foo
        f = \(x : void) -> {print "Got unit"; x}
    in
      {x.foo(());
       f(());
      }
