class Foo
  f : void
  def foo(unit : void) : void
    print "Got unit"

class Main
  def main() : void {
    var unit1 = ();
    val unit2 = ();
    val x = new Foo;
    val f = \(x : void) -> {print "Got unit"; x};
    x.foo(unit1);
    f(unit2);
    unit1 = unit2;
  }