-- This file was automatically converted by encorec

class Main
  var myFun : (String -> void, String) -> void
  def app(f : String -> void, s : String) : void
    let
      myFun = this.myFun
    in
      myFun(f, s)
    end
  end
  def main() : void
    let
      doTwice = fun (f : String -> void, x : String) 
                  f(x)
                  f(x)
                end
    in
      this.myFun = doTwice
      this.app(fun (s : String) => println(s), "Hello ponyworld")
    end
  end
end

