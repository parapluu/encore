class Main
  def main() : void
    let other : Other = new Other in {
       other.mthd_int(10);
       other.mthd_obj(this);
       other.printSum(10, 20, "c")
    }

class Other
  def mthd_int(a : int) : void {
    print string "mthd_int";
    print int a
  }

  def mthd_obj(a : Main) : void {
    print string "mthd_obj"
  
  }

  def printSum(a : int, b : int, t : string) : void {
    print int a+b;
    print string t
  }

