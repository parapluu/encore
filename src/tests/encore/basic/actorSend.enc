class Main 
  def main() : void
    let other : Other = new Other in
      let another : Other = new Other in
        {other.init(another);
         other.work()}

class Other 
  other : Other
  def init(va : Other) : void 
    this.other = va
  def work() : void
    {let othertmp : Other = this.other in
       {othertmp.printMsg()}}
  def printMsg() : void
    print string "Hello Actorworld!"
