-- OK
def <a> testCallToParametricFunction(x: int, y: a): void {
  testCreationOfObject<a, int>(y, x); -- correct trace
  testCallToParametricFunction<a>(x, y); -- correct trace
  testCreationOfObject<int, int>(23, x); -- correct trace
  testCreationOfObject<String, int>("23", x); -- correct trace
  new Test<String>(x, "string");
  new Test<a>(x, y);
}

-- OK
def <a> testCallToFunction(x: int, y: a): void
  testCallToParametricFunction<String>(x, "test") -- correct trace

-- OK
def <a> testCreationOfParametricObject(x: a): void {
  testCreationOfParametricObject<int>(23);
}

--OK
def <b, c> testCreationOfObject(x:b, y:c): void {
  -- new Test<String>;
  ()
}

-- def <a,b> testKiko2(x: a, y: b): (a,b)
--   (x,y)

class NAME<a, b, c>

class Test<a>
  s: a
  def init(x: int, y: a): void
    ()

  def tt(x: a): void {
    new Test<bool>(34, true);
    new [a](10);
  }

class Main
  def main(): void {
    testCreationOfObject<String, int>("sd", 34); -- correct trace
    ()
  }
