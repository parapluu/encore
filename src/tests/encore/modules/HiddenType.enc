import Lib.SelectiveLib

def foo(x : Bar) : void
  ()

class Bar -- Same name as result type of Lib.SelectiveLib.Foo.foo()

class Main
  def main() : void {
    val x = new Foo;
    val y = x.foo();
    foo(y);
  }