-- TEST SIMPLE FORWARD
class Base
  def one() : int
    1806

class Next
  def two() : int
    forward (new Base).one()

  def two_get() : int
    get (new Base).one()

class Main
  def main() : void
    print get (new Next).two()
