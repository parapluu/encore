-- TEST FORWARD IN ONE-WAY SEND
class Base
  def one() : int
    1806

class Next
  def two() : int
    forward (new Base).one()

class Main
  def main() : void {
    (new Next)!two();
    print 10
  }
