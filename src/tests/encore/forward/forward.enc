-- This file was automatically converted by encorec

class Worker
  def work() : int
    42
  end
end
class Lazy
  def work() : int
    if true then
      forward((new Worker).work())
    else
      100
    end
    println("This should never run!")
    0
  end
  def foo() : int
    this.work()
  end
end
class Main
  def main() : void
    println(get((new Lazy).work()))
    (new Lazy)!work()
    (new Lazy)!foo()
    println(get((new Lazy).foo()))
  end
end
