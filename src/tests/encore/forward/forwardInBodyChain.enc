active class Foo
  def foo() : int
    42
  end
end

active class Bar
  def bar() : int
    10
  end
end

active class Main
  def main() : unit
    var g = (new Foo)!foo()
    println(g ~~> fun (x: int) => forward((new Bar)!bar()))
  end
end
