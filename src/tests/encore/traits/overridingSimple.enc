trait T
  require f : int
  def foo() : int
    this.f

passive class Foo : T {
  f : int
  def init(f : int) : void {
    this.f = f;
  }

  def foo() : int {
    this.f + 3;
  }
}

class Main
  def main() : void{
    val x = new Foo(2) : T;
    println("Five = {}", x.foo());
  }
