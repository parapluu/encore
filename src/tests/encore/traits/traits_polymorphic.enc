-- This file was automatically converted by encorec

fun id[a](x : a) : a
  x
end
trait T[t]
  require var x : t
  def foo() : unit
    val arr = new [t](10)
    val x = arr(0) : t
    new Foo[t](x)
    val f = id[t]
    f(x)
    id(x)
    EMBED (t)
      #{x};

    END
  end
end
class Foo[t]
  def init(x : t) : unit
    ()
  end
end
passive class C : T[int]
  var x : int
end
class Main
  def main() : unit
    let
      c = new C
    in
      c.x = 1
      println(c.x)
    end
  end
end
