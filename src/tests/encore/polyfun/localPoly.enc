def foo() : int
  13 + 14 + id(15)
where
  def id<a>(x : a) : a
    x
end

def id<a>(x : a) : a
  myOtherId(myId(x))
where
  def myId(x : a) : a
    x
  def myOtherId<b>(x : b) : b
    x
end

class Main
  def main() : void {
    print id(foo())
  }