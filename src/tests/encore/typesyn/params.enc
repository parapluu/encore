-- This file was automatically converted by encorec

typedef T[x] = Bar[x]
active class Bar[t]
  def id(a : t) : t
    a
  end
end
active class Main
  def main() : unit
    let
      x = new Bar[int] : T[int]
    in
      println(get(x ! id(10)))
    end
  end
end
