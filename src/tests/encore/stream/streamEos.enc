class Foo
  stream bar() : int {
    yield 1;
    eos;
    print "Never come here";
    yield 2;
  }

class Main
  def main() : void {
    var s = (new Foo).bar();
    while (not eos s) {
      print (get s);
      s = getNext s;
    }
  }
