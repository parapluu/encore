active class Main
  def main(args : [String]) : unit
    match Nothing with
      case Nothing =>
        println("Until the typechecker gets clever enough, this won't compile")
      end
    end
  end
end